# Screenshots

Drop the app screenshots here so they render in the project `README.md`.

Expected filenames:

- `empty_state.png` — the empty list screen
- `add_item.png` — the add/edit item form
- `shopping_list.png` — the populated list
- `purchased_item.png` — an item checked off as purchased
